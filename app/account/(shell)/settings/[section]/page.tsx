/**
 * Section body is rendered by `MovynAccountDashboardShell` from the URL.
 * This route exists so `/account/settings/:section` is a real segment (kit-style settings URLs).
 */
export default function AccountSettingsSectionPage() {
  return null;
}
