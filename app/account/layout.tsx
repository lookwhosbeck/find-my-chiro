import type { ReactNode } from 'react';

/** Root account segment: route group `(shell)` supplies the dashboard chrome. */
export default function AccountRootLayout({ children }: { children: ReactNode }) {
  return children;
}
